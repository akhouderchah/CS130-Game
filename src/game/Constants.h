#pragma once

#include <string>

// Global constants //
const float FLOOR_PERCENTAGE = 1.5f/15;

const float HORIZONTAL_SPEED = 350.f;

const float SPAWN_INTERVAL_MIN = 400.f;
const float SPAWN_INTERVAL_MAX = 700.f;

// Player constants //
const float PLAYER_SIZE = 10.5f;
const float PLAYER_OUTLINE_SIZE = 5.f;
const float PLAYER_JUMP_VELOCITY = -400.f;
const float PLAYER_MAX_VELOCITY = 400.f;
const float PLAYER_INVINCIBLE_TIME = 1.f;

// Text constants //
const std::string PAUSE_STRING = "Paused";
const std::string OVER_STRING = "Press any key to restart";

